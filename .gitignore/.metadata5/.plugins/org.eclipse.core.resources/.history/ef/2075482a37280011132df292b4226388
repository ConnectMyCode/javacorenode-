package com.inheritance.learn;

import java.util.Scanner;

public class HasARelationAgggregationDemo12 {

	static Scanner cs = new Scanner(System.in);
	
	public static void main(String []args) 
	{
		String clgName ; 
		String location; 
		
		System.out.println("enter college Name: ");  
		clgName = cs.nextLine();  
		
		System.out.println("enter college location: ");  
		location = cs.nextLine();  
		
								//(String , String )
		//College Object Created (College_Name , location) 
		HasARelationAgggregationCollegeDemo12 clgs = new HasARelationAgggregationCollegeDemo12(clgName,location);
		System.out.println(clgs);
		
		int id; 
		String studName; 
		String address; 
		
		System.out.println("enter student id: ");  
		id = cs.nextInt();
		cs.nextLine(); //after nextInt there is a extraw , "\n" character which is consumed by this .nextLine() so it does not take the user input and goes  to the next line
		System.out.println("enter Student Name: ");  
		studName = cs.nextLine();
		
		System.out.println("enter College Address: ");  
		address = cs.nextLine();
		
		cs.close();
		
								//(int ,String , String , College)
		//Student Object created  ( id ,name , address , clg ); 
		HasARelationAgggregationStudentDemo12 stud1 = new HasARelationAgggregationStudentDemo12(id, studName ,address,clgs); 
		System.out.println(stud1); 
		
		
		
		//Deleting the Container to verify if the contained object is deleted or not that  means it is independent or not of the contaner object: Aggregation HAS-A Relation
		
		
		
		
	}
	
	
}
