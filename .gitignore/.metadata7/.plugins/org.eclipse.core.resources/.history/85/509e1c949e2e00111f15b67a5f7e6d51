package com.objectclass.methods.learn;

public class EqualsMethodDemo2 {

	public static void main(String []args) {
		
		EqualsMethodEmployeeDemo2 e1= new EqualsMethodEmployeeDemo2(20,"Bonitaflakes" );
	    
		EqualsMethodEmployeeDemo2 e2= new EqualsMethodEmployeeDemo2(20,"Bonitaflakes" );
		//Above 2 diff objects are created having same content , So this must be smae object 
		//we can compare these 2 object based on ontent bby overiding the --> 1/equals()  and 2/hashcode()    becuase there is some contract in between the   hashmap() and equals()/bonitaflakes method  so we need to override  both the methods when we are overriding the hashmap() method. 
		
		
		System.out.println(e1.equals(e2)); //True
		
		
		System.out.println(e1.hashcode()+ "  :  "+e2.hashode());
		
		
		
		
		
		
		
	}
	
}
